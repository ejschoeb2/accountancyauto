---
phase: 13-onboarding-flow-team-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/supabase/middleware.ts
  - app/(auth)/onboarding/layout.tsx
  - app/(auth)/onboarding/page.tsx
  - app/(auth)/onboarding/actions.ts
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05, ONBD-06]

must_haves:
  truths:
    - "A new user can enter their email on the /onboarding page without being logged in first"
    - "After magic link verification, user can enter firm name and URL slug with real-time availability check"
    - "User sees all 4 plan tiers and can start a 14-day free trial"
    - "After completing the wizard, user is redirected to their org's subdomain dashboard with valid JWT claims"
    - "A user who already has an org is redirected away from /onboarding to their dashboard"
    - "/onboarding is accessible without org context (no slug required)"
  artifacts:
    - path: "app/(auth)/onboarding/page.tsx"
      provides: "4-step wizard: Account, Firm Details, Plan Selection, Trial Started"
      min_lines: 100
    - path: "app/(auth)/onboarding/actions.ts"
      provides: "createOrgAndJoinAsAdmin, checkSlugAvailable server actions"
      exports: ["createOrgAndJoinAsAdmin", "checkSlugAvailable"]
    - path: "app/(auth)/onboarding/layout.tsx"
      provides: "Layout handling auth state and already-onboarded redirect"
      min_lines: 20
    - path: "lib/supabase/middleware.ts"
      provides: "Updated PUBLIC_ROUTES with /onboarding and /invite/accept"
      contains: "/onboarding"
  key_links:
    - from: "app/(auth)/onboarding/page.tsx"
      to: "app/(auth)/onboarding/actions.ts"
      via: "server action calls"
      pattern: "createOrgAndJoinAsAdmin|checkSlugAvailable"
    - from: "app/(auth)/onboarding/actions.ts"
      to: "lib/billing/trial.ts"
      via: "createTrialForOrg import"
      pattern: "createTrialForOrg"
    - from: "app/(auth)/onboarding/page.tsx"
      to: "supabase.auth.refreshSession"
      via: "client-side session refresh before subdomain redirect"
      pattern: "refreshSession"
---

<objective>
Build the 4-step onboarding wizard that allows a new user to sign up, create an organisation, select a plan (14-day free trial), and land on their org's subdomain dashboard.

Purpose: This is the primary acquisition funnel for new accounting firms. Without it, no new tenants can be created.
Output: Working onboarding flow at `/onboarding` on the bare domain, creating a fully provisioned org with trial subscription.
</objective>

<execution_context>
@C:/Users/ejsch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ejsch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-onboarding-flow-team-management/13-RESEARCH.md

@lib/supabase/middleware.ts
@app/(auth)/onboarding/layout.tsx
@app/(auth)/onboarding/page.tsx
@app/(auth)/login/actions.ts
@components/wizard-stepper.tsx
@lib/billing/trial.ts
@lib/stripe/plans.ts
@lib/auth/org-context.ts
@lib/supabase/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update middleware PUBLIC_ROUTES and rewrite onboarding layout</name>
  <files>
    lib/supabase/middleware.ts
    app/(auth)/onboarding/layout.tsx
  </files>
  <action>
**Middleware update (`lib/supabase/middleware.ts`):**
- Add `/onboarding` and `/invite/accept` to the `PUBLIC_ROUTES` array.
- These routes must pass through the middleware without requiring org context or authentication (Step 3 of middleware handles this already — public routes return `supabaseResponse` after session refresh).
- This is a one-line change: add the two strings to the existing array.

**Onboarding layout rewrite (`app/(auth)/onboarding/layout.tsx`):**
- The current layout requires authentication (redirects to /login if no user). This must change because onboarding Step 1 is for unauthenticated users entering their email.
- Remove the auth check from the layout entirely. Authentication state is handled by the wizard page itself (Step 1 = unauthenticated, Steps 2-4 = authenticated).
- Keep the existing visual layout: centered full-page, branding logos at top.
- Add "already onboarded" redirect: After checking for a user (optional — layout renders regardless), if user exists, use admin client to check if user has a `user_organisations` row. If yes, query their org slug and redirect to their org subdomain (production) or `?org=slug` URL (development). This prevents re-entry per ONBD-06.
- The redirect logic is server-side in the layout: `const admin = createAdminClient()` → query `user_organisations` → if found, query `organisations.slug` → `redirect(orgUrl)`.
- If no user or no org membership, render children (the wizard page).
- Keep the branding section (logos) from the current layout.

**Important:** Do NOT remove the existing `app/(auth)/onboarding/components/` directory files. They will be unused by the new wizard but removing them is unnecessary churn. The new page.tsx simply won't import them.
  </action>
  <verify>
- `npx tsc --noEmit` passes
- Confirm `PUBLIC_ROUTES` array includes "/onboarding" and "/invite/accept"
- Confirm layout no longer has a hard auth redirect before rendering children
  </verify>
  <done>
- `/onboarding` is reachable without authentication and without an org slug
- Users with an existing org are redirected away from `/onboarding`
- Layout renders branding and children for users without an org
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 4-step onboarding wizard page and server actions</name>
  <files>
    app/(auth)/onboarding/page.tsx
    app/(auth)/onboarding/actions.ts
  </files>
  <action>
**Server actions (`app/(auth)/onboarding/actions.ts`):**

Create a new `actions.ts` with `"use server"` directive containing:

1. `checkSlugAvailable(slug: string): Promise<{ available: boolean; reason?: string }>`:
   - Validate slug format: must match `/^[a-z0-9][a-z0-9-]*[a-z0-9]$/` and be at least 3 characters. If single character, also accept `/^[a-z0-9]$/`.
   - Check against reserved slug list: `["www", "app", "api", "admin", "billing", "onboarding", "invite", "pricing", "login", "dashboard", "clients", "settings", "templates", "schedules", "email-logs", "rollover"]`.
   - Query `organisations` table via admin client for existing slug.
   - Return `{ available: true }` or `{ available: false, reason: "..." }`.

2. `createOrgAndJoinAsAdmin(firmName: string, slug: string, planTier: PlanTier): Promise<{ orgId: string; slug: string }>`:
   - Get the current authenticated user via `createClient()` → `supabase.auth.getUser()`. Throw if not authenticated.
   - Use `createAdminClient()` for all DB writes (user has no org_id in JWT yet — RLS would block).
   - Check the user does not already have a `user_organisations` row (prevent double-create).
   - INSERT into `organisations`: `{ name: firmName, slug, plan_tier: planTier }`. Select the created row.
   - INSERT into `user_organisations`: `{ user_id: user.id, org_id: org.id, role: "admin" }`.
   - Call `createTrialForOrg(org.id, admin)` from `lib/billing/trial.ts` to set trial_ends_at and subscription_status.
   - Upsert `app_settings`: `{ org_id: org.id, key: "onboarding_complete", value: "true" }` with `onConflict: "org_id,key"`.
   - Return `{ orgId: org.id, slug: org.slug }`.

3. `sendOnboardingMagicLink(email: string): Promise<{ error?: string }>`:
   - Create Supabase client via `createClient()`.
   - Call `signInWithOtp({ email, options: { emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/onboarding" } })`.
   - The emailRedirectTo points back to `/onboarding` (not the auth callback) so the user lands back on the wizard after clicking the magic link. In production, use `https://app.phasetwo.uk/onboarding` (bare domain, no subdomain — org doesn't exist yet).
   - Wait — this needs careful consideration. After clicking the magic link, Supabase redirects to the `emailRedirectTo` URL with a `code` query param. The existing `/auth/callback` route handles the code exchange. So the emailRedirectTo must go through `/auth/callback` first.
   - Actually, use `emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/auth/callback?next=/onboarding"` (add a `next` param so the callback knows to redirect to `/onboarding` after exchange instead of the dashboard).
   - Wait — the existing auth callback redirect logic in `app/(auth)/auth/callback/route.ts` redirects to the org's subdomain after exchange. For onboarding users, there is no org yet. So the callback must handle the case where the user has no org_id in their JWT.
   - **Revised approach:** Use `emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/auth/callback"`. The auth callback already handles users without org_id — in that case, the middleware's no-slug logic redirects them. Since `/onboarding` is in PUBLIC_ROUTES, users who complete the magic link flow and have no org will end up at the callback, which should redirect to `/onboarding` if no org is found.
   - Actually, looking at the callback code (12-03), after code exchange it tries to find the user's org slug from JWT. If no org_id, it falls back to `user_organisations` query. If the user has no org yet, this returns null. The callback then falls through to a default redirect to "/" which the middleware handles.
   - **Simplest approach:** The `emailRedirectTo` for onboarding should be `${NEXT_PUBLIC_APP_URL}/onboarding`. Supabase will redirect to this URL with a `code` param. BUT the onboarding page is a client component — it cannot handle the code exchange server-side.
   - **FINAL approach:** Use `emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/auth/callback"` (same as login). After code exchange, the callback checks for org_id. If no org_id, it redirects to "/" with no slug. The middleware sees an authenticated user with no slug and no org → in dev, redirects to /login; in prod, redirects to marketing site. This won't work for onboarding.
   - **ACTUAL FINAL approach:** Modify the auth callback route to check if user has no org: if authenticated and no `user_organisations` row, redirect to `/onboarding` instead of marketing site. This is a minimal change to the callback. BUT this file is not in this plan's `files_modified`. Let me reconsider.
   - **Pragmatic approach:** Use `emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/onboarding"`. The Supabase magic link will redirect to `/onboarding?code=...`. Add code exchange handling directly in the onboarding page: if `searchParams.code` exists, call `supabase.auth.exchangeCodeForSession(code)` before rendering the wizard. This is the same pattern as the auth callback but inline. Since the onboarding layout no longer blocks unauthenticated users, and the page itself handles auth state per step, this works cleanly.
   - Actually, the exchange MUST happen server-side for cookie setting. The onboarding page is a client component ("use client"). Code exchange in a client component won't set the server-side cookies correctly.
   - **SIMPLEST CORRECT approach:** Create a small route handler at `app/(auth)/onboarding/callback/route.ts` that does the code exchange and redirects to `/onboarding`. Or better: just use the existing auth callback but handle no-org case there. Since we're already modifying `lib/supabase/middleware.ts` in this task, add a special case in the middleware: if authenticated user has no org and pathname is not `/onboarding`, redirect to `/onboarding`. This way the auth callback's default redirect to "/" gets caught by middleware and sent to `/onboarding`.
   - **Wait, simplify:** The middleware already has no-slug handling (Step 5). Currently, an authenticated user with no slug who has no org gets redirected to marketing site. Change this: if user has no `user_organisations` row, redirect to `/onboarding` instead of marketing site. This is a 2-line change in `middleware.ts` (Step 5 fallback). Then `emailRedirectTo` can simply be `${NEXT_PUBLIC_APP_URL}/auth/callback` and everything flows naturally: magic link → callback → exchange → redirect to "/" → middleware sees no org → redirect to `/onboarding`.
   - **This is the approach.** Update Task 1's middleware changes to also add: in the no-slug authenticated fallback (line ~121 where it redirects to phasetwo.uk when no org found), instead redirect to `/onboarding`.

So `sendOnboardingMagicLink` is simple:
   - `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: "${NEXT_PUBLIC_APP_URL}/auth/callback" } })`.
   - Return `{ error }` or `{ success: true }`.

**IMPORTANT middleware addition to Task 1:** In the no-slug authenticated user fallback (after failing to find a user_organisations row), redirect to `/onboarding` instead of `https://phasetwo.uk`. Specifically, change the fallback at line ~121 (`return NextResponse.redirect(new URL("https://phasetwo.uk"), 307)`) to redirect to `/onboarding` when the user has no org. This ensures users who complete magic link auth during onboarding land back on the wizard.

**Wizard page (`app/(auth)/onboarding/page.tsx`):**

Replace the entire page with a new "use client" component implementing a 4-step wizard:

- Use `useState` for current step: `"account" | "firm" | "plan" | "done"`.
- Track authentication state: On mount, check `supabase.auth.getUser()` via browser client. If authenticated, skip to Step 2 (Firm Details).
- Reuse `<WizardStepper>` component from `components/wizard-stepper.tsx` with steps: `["Account", "Firm Details", "Plan", "Trial Started"]`.

**Step 1 — Account (unauthenticated):**
- Email input field + "Send Magic Link" button.
- Call `sendOnboardingMagicLink(email)` server action.
- On success, show "Check your email" message with the email address displayed.
- User clicks the magic link in email → goes through auth callback → middleware redirects to `/onboarding` → page loads, detects authenticated user, auto-advances to Step 2.

**Step 2 — Firm Details (authenticated):**
- Firm name input (required).
- URL slug input with debounced availability check (call `checkSlugAvailable` on each change after 500ms delay). Show green checkmark or red X with reason.
- Auto-suggest slug from firm name: lowercase, replace spaces with hyphens, remove special chars. User can edit freely.
- "Continue" button enabled only when firm name is non-empty and slug is available.
- "Back" button returns to Step 1 (though user is already authenticated, they can re-enter email info).

**Step 3 — Plan Selection (authenticated):**
- Display all 4 plan tiers from `PLAN_TIERS` config (import from `lib/stripe/plans.ts`).
- Show plan cards with: name, price (format as £XX/mo from monthlyPrice pence), client limit, user limit, features list.
- Each card has a "Start Free Trial" button (per user decision — trial only, no payment upfront).
- Practice tier highlighted as "Popular" with a primary ring (same pattern as existing pricing page).
- Clicking "Start Free Trial" on any tier calls `createOrgAndJoinAsAdmin(firmName, slug, tier)`.
- On success, advance to Step 4.
- "Back" button returns to Step 2.

**Step 4 — Trial Started (authenticated, org created):**
- Confirmation page showing: green checkmark icon, "Your trial has started!" heading, plan name, "14 days remaining", firm name.
- "Go to Dashboard" button that:
  1. Calls `supabase.auth.refreshSession()` to trigger JWT hook with new org_id.
  2. Redirects to org subdomain: in production `https://${slug}.app.phasetwo.uk/dashboard`, in dev `/?org=${slug}` (same pattern as auth callback).
- Use the browser Supabase client for `refreshSession()`.

**Design notes (Claude's discretion):**
- Use shadcn Card components for plan cards and form sections.
- Use existing Tailwind patterns from the codebase.
- Keep the full-page centered layout from the onboarding layout.
- Loading states use `Loader2` spinner (existing pattern).
- Error messages shown inline with red text.

**ONBD-03 note:** The user's locked decision simplified Step 2 to "firm name and URL slug only." Postmark configuration (sender name, email, token) is already available post-onboarding in the Settings page via `PostmarkSettingsCard`. ONBD-03 is satisfied: sender details can be configured later in Settings per the requirement's own text ("optional — can be configured later in Settings").
  </action>
  <verify>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Manually verify at `localhost:3000/onboarding`:
  - Page loads without authentication
  - Email input and "Send Magic Link" button visible on Step 1
  - After auth, Step 2 shows firm name + slug inputs
  - Slug availability check returns results
  </verify>
  <done>
- 4-step wizard renders correctly with progress bar
- Magic link can be sent from Step 1
- Slug availability is validated in real-time on Step 2
- createOrgAndJoinAsAdmin creates org, user_org, and trial
- Step 4 refreshes JWT session and redirects to org subdomain
- Users with existing orgs are redirected away from /onboarding (ONBD-06)
- /onboarding works on bare domain without slug (ONBD-05 path)
  </done>
</task>

</tasks>

<verification>
1. Navigate to `/onboarding` without authentication — wizard Step 1 (Account) loads with email input
2. Enter email, receive magic link, click it — returns to /onboarding at Step 2 (Firm Details)
3. Enter firm name, slug auto-suggests, availability check shows green/red feedback
4. Select a plan tier, click "Start Free Trial" — org is created in database with trial_ends_at set
5. Step 4 shows confirmation, "Go to Dashboard" refreshes session and redirects to org subdomain
6. Navigating back to `/onboarding` after completion redirects to dashboard (re-entry blocked)
7. `npx tsc --noEmit` and `npm run build` pass
</verification>

<success_criteria>
- New user can go from zero to org dashboard via the 4-step wizard
- Organisation row created with correct plan_tier, trial_ends_at, subscription_status="trialing"
- user_organisations row created with role="admin"
- app_settings row with onboarding_complete=true exists
- JWT contains org_id after session refresh on Step 4
- Already-onboarded users cannot re-enter the wizard
</success_criteria>

<output>
After completion, create `.planning/phases/13-onboarding-flow-team-management/13-01-SUMMARY.md`
</output>
