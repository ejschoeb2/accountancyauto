---
phase: 11-stripe-billing
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - app/(dashboard)/billing/page.tsx
  - app/(dashboard)/billing/components/billing-status-card.tsx
  - app/(dashboard)/billing/components/usage-bars.tsx
  - app/(dashboard)/billing/components/manage-billing-button.tsx
autonomous: true

must_haves:
  truths:
    - "Billing page shows current plan name, subscription status, and next billing date"
    - "Billing page shows client count vs limit as a usage bar with percentage"
    - "Billing page shows user count vs limit (display only, enforcement deferred to Phase 13)"
    - "80% usage warning appears on billing page when client count is at or above 80% of limit"
    - "'Manage billing' button opens Stripe Customer Portal pre-authenticated for the org"
    - "Only org admins can access the billing page; members cannot see it"
    - "Trial orgs see trial status and expiry date on billing page"
  artifacts:
    - path: "app/(dashboard)/billing/page.tsx"
      provides: "Billing management page (admin-only)"
    - path: "app/(dashboard)/billing/components/billing-status-card.tsx"
      provides: "Status overview card showing plan, status, billing date"
    - path: "app/(dashboard)/billing/components/usage-bars.tsx"
      provides: "Usage bar components for client and user counts"
    - path: "app/(dashboard)/billing/components/manage-billing-button.tsx"
      provides: "Button that creates portal session and redirects to Stripe"
  key_links:
    - from: "app/(dashboard)/billing/page.tsx"
      to: "lib/billing/usage-limits.ts"
      via: "getUsageStats call for usage bars"
      pattern: "getUsageStats"
    - from: "app/(dashboard)/billing/components/manage-billing-button.tsx"
      to: "app/api/stripe/create-portal-session/route.ts"
      via: "fetch POST to create portal session"
      pattern: "create-portal-session"
    - from: "app/(dashboard)/billing/page.tsx"
      to: "lib/auth/org-context.ts"
      via: "getOrgContext for admin check"
      pattern: "getOrgContext"
---

<objective>
Build the billing management page showing current plan status, usage metrics with progress bars, and a "Manage billing" button linking to Stripe Customer Portal.

Purpose: This is the org admin's single view into their subscription status, usage against limits, and entry point to Stripe Portal for all billing changes. Per user decision, this page is admin-only, and all plan changes go through the Stripe Portal.

Output: /billing page with status card, usage bars, manage billing button
</objective>

<execution_context>
@C:\Users\ejsch\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ejsch\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-stripe-billing/11-RESEARCH.md
@.planning/phases/11-stripe-billing/11-CONTEXT.md
@.planning/phases/11-stripe-billing/11-01-SUMMARY.md
@.planning/phases/11-stripe-billing/11-03-SUMMARY.md
@lib/billing/usage-limits.ts
@lib/billing/read-only-mode.ts
@lib/stripe/plans.ts
@lib/auth/org-context.ts
@app/(dashboard)/settings/page.tsx
@app/(dashboard)/layout.tsx
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create billing management page with status overview</name>
  <files>
    app/(dashboard)/billing/page.tsx
    app/(dashboard)/billing/components/billing-status-card.tsx
    app/(dashboard)/billing/components/usage-bars.tsx
    app/(dashboard)/billing/components/manage-billing-button.tsx
  </files>
  <action>
    1. Create `app/(dashboard)/billing/page.tsx` (server component):
       - Import getOrgContext from '@/lib/auth/org-context'
       - Import getUsageStats from '@/lib/billing/usage-limits'
       - Import PLAN_TIERS, getPlanByTier from '@/lib/stripe/plans'
       - Check admin access: call getOrgContext(), if orgRole !== 'admin', use redirect() to '/dashboard' (per user decision: "Org admins only -- members cannot see billing page at all")
       - Fetch org data from Supabase (using authenticated client, RLS scoped):
         - Select: plan_tier, subscription_status, trial_ends_at, stripe_customer_id, stripe_subscription_id, client_count_limit, user_count_limit
       - Fetch usage stats via getUsageStats(orgId)
       - Get plan config via getPlanByTier(org.plan_tier)
       - Render page layout (following settings page pattern -- `max-w-7xl mx-auto space-y-8`):
         - Page header: "Billing" with subtitle "Manage your subscription and usage"
         - BillingStatusCard: plan name, subscription status, trial info
         - UsageBars: client count vs limit, user count vs limit
         - ManageBillingButton: opens Stripe Customer Portal
         - If no subscription (no stripe_customer_id): show "No active subscription" state with link to /pricing

    2. Create `app/(dashboard)/billing/components/billing-status-card.tsx`:
       - Receives props: planName, subscriptionStatus, trialEndsAt (Date | null), monthlyPrice
       - Display in a Card component:
         - Plan name with badge (e.g., "Practice Plan")
         - Subscription status with color coding:
           - 'active' = green badge
           - 'trialing' = blue badge with "Trial ends {date}"
           - 'past_due' = amber badge "Payment overdue"
           - 'cancelled' = red badge "Cancelled"
           - 'unpaid' = red badge "Unpaid"
         - Monthly price: "£XX/mo" (exclusive of VAT)
         - If trialing: show trial expiry date formatted as "Trial ends DD MMM YYYY" and days remaining
       - Per user decision: "Trial status shown on billing page only -- no persistent banner during trial"

    3. Create `app/(dashboard)/billing/components/usage-bars.tsx`:
       - Receives props: clientCount, clientLimit (number | null), userCount (number, currently always 1), userLimit (number | null)
       - For each metric, render:
         - Label: "Clients" or "Team members"
         - Progress bar (Tailwind div with width percentage)
         - Count text: "{current} / {limit}" or "{current} / Unlimited"
         - Percentage text: "XX%"
       - Color coding for progress bar:
         - 0-79%: default (blue/neutral)
         - 80-99%: amber/warning
         - 100%: red/danger
       - Per user decision: "80% usage warning appears on billing page only (not dashboard banner)"
         - When at >= 80%, show warning text below the bar: "You're approaching your client limit. Consider upgrading your plan."
         - When at 100%: "You've reached your client limit. Upgrade your plan to add more clients." with link to /pricing
       - User count bar shows current user count vs limit (display only in Phase 11; enforcement is Phase 13)

    4. Create `app/(dashboard)/billing/components/manage-billing-button.tsx`:
       - Client component ('use client')
       - Receives prop: orgId, hasSubscription (boolean)
       - If hasSubscription:
         - "Manage billing" button (primary variant)
         - On click: POST to /api/stripe/create-portal-session with { orgId }
         - On success: window.location.href = response.url (redirect to Stripe Portal)
         - Show loading state during API call
       - If !hasSubscription:
         - "Choose a plan" button linking to /pricing
       - Per user decision: "Invoice history handled entirely by Stripe Portal -- no in-app invoice list"

    Follow existing design patterns from DESIGN.md and settings page:
    - Use Card, CardHeader, CardContent from shadcn/ui
    - Consistent spacing (space-y-8 between sections)
    - Button variants consistent with existing UI
    - Use existing Tailwind utility classes
  </action>
  <verify>
    - `/billing` page renders with status card, usage bars, and manage button
    - Admin check: non-admin redirected to /dashboard
    - Status card shows plan name and subscription status with appropriate badge colors
    - Usage bars show client count vs limit with percentage
    - 80% threshold triggers warning text on billing page
    - Manage billing button POSTs to portal session endpoint
    - TypeScript compiles: `npx tsc --noEmit --pretty 2>&1 | head -20`
  </verify>
  <done>
    Billing page shows plan status, usage bars with 80%/100% warnings, trial status, and "Manage billing" button linking to Stripe Customer Portal; admin-only access enforced
  </done>
</task>

</tasks>

<verification>
- /billing page accessible only to org admins (members redirected)
- Status card shows plan name, subscription status badge, trial expiry if applicable
- Usage bars show client count vs limit with percentage and color coding
- 80% warning text appears when client usage >= 80%
- 100% limit text appears with upgrade link when at limit
- "Manage billing" button creates portal session and redirects to Stripe
- No subscription state shows "Choose a plan" linking to /pricing
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
1. Billing page displays current plan, status, and pricing
2. Usage bars accurately reflect client count vs plan limit
3. 80% threshold warning visible on billing page
4. Manage billing opens Stripe Customer Portal
5. Admin-only access enforced (members cannot see page)
6. Trial status shown with expiry date
</success_criteria>

<output>
After completion, create `.planning/phases/11-stripe-billing/11-04-SUMMARY.md`
</output>
