---
phase: 14-super-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/supabase/middleware.ts
  - components/nav-links.tsx
  - app/(dashboard)/layout.tsx
  - app/(dashboard)/admin/page.tsx
  - app/(dashboard)/admin/loading.tsx
  - app/(dashboard)/admin/components/org-table.tsx
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "A user with app_metadata.is_super_admin = true can access /admin and sees a list of all organisations"
    - "A regular org user navigating to /admin is silently redirected to /dashboard"
    - "The Admin nav link appears only for super-admin users"
    - "The org list shows name, slug, plan tier, subscription status, trial expiry, client count, and user count for every org"
    - "The org list is sortable by clicking column headers on plan tier and subscription status"
    - "Subscription status is displayed as color-coded badges using the existing traffic light system"
    - "Trial expiry shows relative days remaining ('12 days left', 'Expired 3 days ago')"
  artifacts:
    - path: "lib/supabase/middleware.ts"
      provides: "Admin route bypass skipping org-membership and subscription enforcement"
      contains: "isAdminRoute"
    - path: "components/nav-links.tsx"
      provides: "Conditional Admin nav link for super-admin users"
      contains: "isSuperAdmin"
    - path: "app/(dashboard)/admin/page.tsx"
      provides: "Server component with super-admin guard and cross-org data fetch"
      contains: "createAdminClient"
    - path: "app/(dashboard)/admin/components/org-table.tsx"
      provides: "Client-side sortable table with TanStack Table"
      contains: "useReactTable"
  key_links:
    - from: "app/(dashboard)/layout.tsx"
      to: "components/nav-links.tsx"
      via: "isSuperAdmin prop from user.app_metadata"
      pattern: "is_super_admin.*NavLinks"
    - from: "app/(dashboard)/admin/page.tsx"
      to: "lib/supabase/admin.ts"
      via: "createAdminClient for cross-org queries"
      pattern: "createAdminClient"
    - from: "lib/supabase/middleware.ts"
      to: "app/(dashboard)/admin/page.tsx"
      via: "isAdminRoute bypass allowing access without org membership check"
      pattern: "isAdminRoute.*pathname"
---

<objective>
Build the super-admin route infrastructure (middleware bypass, nav link, page guard) and the org list page with a sortable data table showing all organisations with their plan, status, usage, and health.

Purpose: Give the platform operator a single dashboard to monitor all tenants without accessing the production database.
Output: `/admin` route accessible only to super-admin users, showing a sortable org list with all required columns and status badges.
</objective>

<execution_context>
@C:/Users/ejsch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ejsch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-super-admin-dashboard/14-CONTEXT.md
@.planning/phases/14-super-admin-dashboard/14-RESEARCH.md
@DESIGN.md
@lib/supabase/middleware.ts
@lib/supabase/admin.ts
@components/nav-links.tsx
@app/(dashboard)/layout.tsx
@app/(dashboard)/billing/components/billing-status-card.tsx
@app/(dashboard)/clients/components/client-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware admin route bypass and NavLinks super-admin prop</name>
  <files>
    lib/supabase/middleware.ts
    components/nav-links.tsx
    app/(dashboard)/layout.tsx
  </files>
  <action>
**Middleware bypass (`lib/supabase/middleware.ts`):**

Add an `isAdminRoute()` helper function:
```typescript
function isAdminRoute(pathname: string): boolean {
  return pathname.startsWith("/admin");
}
```

Insert an admin route check AFTER the public/API route checks (after Step 3, before Step 4) that:
1. If `isAdminRoute(pathname)` is true AND `!user`, redirect to `/login`
2. If `isAdminRoute(pathname)` is true AND `user` exists, return `supabaseResponse` immediately (skip Steps 4-9: org resolution, membership validation, subscription enforcement)
3. The page-level guard in the admin page handles the `is_super_admin` check — middleware just ensures authentication

This is a small, safe insertion. Do NOT modify any existing middleware logic for non-admin routes.

**NavLinks prop (`components/nav-links.tsx`):**

1. Add `isSuperAdmin?: boolean` prop to the `NavLinks` component
2. Add a `ADMIN_ITEM` constant: `{ href: "/admin", icon: Shield, label: "Admin" }` — import `Shield` from lucide-react
3. Conditionally append `ADMIN_ITEM` to the nav items list when `isSuperAdmin` is true
4. Place the Admin link AFTER the Billing link (last in the list)

**Layout prop passing (`app/(dashboard)/layout.tsx`):**

1. After the existing `const { data: { user } } = await supabase.auth.getUser()` call, extract: `const isSuperAdmin = user?.app_metadata?.is_super_admin === true;`
2. Pass `isSuperAdmin={isSuperAdmin}` to the `<NavLinks />` component

Note on ADMN-01: The `is_super_admin` flag already lives in Supabase Auth `app_metadata` and is writable only via service role (no application API exposes app_metadata writes). This requirement is satisfied by the existing architecture — no new code needed for the flag itself. The guard and nav changes here enforce the access pattern.
  </action>
  <verify>
1. Run `npx next build` (or `npm run build`) — ensure no TypeScript errors
2. Manually check: A user WITHOUT `is_super_admin` in app_metadata should NOT see an "Admin" link in the nav bar
3. Navigating to `/admin` without authentication should redirect to `/login`
4. Navigating to `/admin` as a non-super-admin should redirect to `/dashboard` (handled by page guard in Task 2)
  </verify>
  <done>
Middleware allows authenticated users through to /admin routes without org membership checks. NavLinks conditionally shows the Admin link. Layout passes isSuperAdmin prop. No existing route behavior is changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin org list page with sortable table, status badges, and trial expiry</name>
  <files>
    app/(dashboard)/admin/page.tsx
    app/(dashboard)/admin/loading.tsx
    app/(dashboard)/admin/components/org-table.tsx
  </files>
  <action>
**Server page (`app/(dashboard)/admin/page.tsx`):**

Create a React Server Component that:

1. **Super-admin guard:** Call `createClient()`, get session via `supabase.auth.getSession()`. If `!session?.user.app_metadata?.is_super_admin`, call `redirect("/dashboard")`. This is the enforcement point for ADMN-02.

2. **Data fetch using admin client:** Call `createAdminClient()` from `@/lib/supabase/admin` (service role, bypasses RLS). Fetch:
   - All orgs: `admin.from("organisations").select("id, name, slug, plan_tier, subscription_status, trial_ends_at, stripe_subscription_id, client_count_limit, user_count_limit, created_at").order("created_at", { ascending: true })`
   - Client counts per org: Use `Promise.all()` with `.select("id", { count: "exact", head: true }).eq("org_id", org.id)` on the `clients` table for each org
   - User counts per org: Same pattern on `user_organisations` table

3. **Combine data:** Map orgs to include `clientCount` and `userCount` numbers.

4. **Render:** Page header with title "Admin" and description "Platform overview — all organisations". Pass the combined data to the `<OrgTable />` client component.

**Loading skeleton (`app/(dashboard)/admin/loading.tsx`):**

Simple skeleton matching the existing pattern — a pulsing placeholder for the page header and a table skeleton with 5 rows of grey bars.

**Client component (`app/(dashboard)/admin/components/org-table.tsx`):**

Create a `"use client"` component using TanStack Table (`@tanstack/react-table`), following the existing `client-table.tsx` pattern. This component receives the org data as props.

Column definitions:
| Column | Header | Cell Renderer | Sortable |
|--------|--------|---------------|----------|
| `name` | Name | Plain text | No |
| `slug` | Slug | Monospace text (`font-mono text-sm text-muted-foreground`) | No |
| `plan_tier` | Plan | Capitalised, formatted (e.g., `sole_trader` → `Sole Trader`) | Yes |
| `subscription_status` | Status | Color-coded badge using DESIGN.md `div+span` pattern (see STATUS_CONFIG below) | Yes |
| `trial_ends_at` | Trial | Relative days display (see formatTrialExpiry below) — only show value when `subscription_status === 'trialing'`, otherwise show `—` | No |
| `clientCount` | Clients | Number, right-aligned | No |
| `userCount` | Users | Number, right-aligned | No |

**STATUS_CONFIG** — reuse the exact mapping from `billing-status-card.tsx`:
```typescript
const STATUS_CONFIG: Record<string, { label: string; bgClass: string; textClass: string }> = {
  active:    { label: "Active",           bgClass: "bg-green-500/10",       textClass: "text-green-600" },
  trialing:  { label: "Trial",            bgClass: "bg-blue-500/10",        textClass: "text-blue-500" },
  past_due:  { label: "Payment overdue",  bgClass: "bg-amber-500/10",       textClass: "text-amber-600" },
  cancelled: { label: "Cancelled",        bgClass: "bg-destructive/10",     textClass: "text-destructive" },
  unpaid:    { label: "Unpaid",           bgClass: "bg-destructive/10",     textClass: "text-destructive" },
};
```
Default for null/unknown: `{ label: "No subscription", bgClass: "bg-status-neutral/10", textClass: "text-status-neutral" }`

**formatTrialExpiry** function:
```typescript
function formatTrialExpiry(trialEndsAt: string | null): string | null {
  if (!trialEndsAt) return null;
  const now = new Date();
  const end = new Date(trialEndsAt);
  const diffDays = Math.ceil((end.getTime() - now.getTime()) / (1000 * 60 * 60 * 24));
  if (diffDays > 0) return `${diffDays} day${diffDays === 1 ? "" : "s"} left`;
  if (diffDays === 0) return "Expires today";
  const ago = Math.abs(diffDays);
  return `Expired ${ago} day${ago === 1 ? "" : "s"} ago`;
}
```

**Sorting:** Use `getCoreRowModel()` and `getSortedRowModel()` from TanStack Table. Only `plan_tier` and `subscription_status` columns have `enableSorting: true`. All other columns have `enableSorting: false`. Render a sort indicator (arrow up/down from lucide) next to sortable column headers. Clicking a sortable header cycles: unsorted → ascending → descending → unsorted.

**Row click:** Each row is a `<Link>` (or `onClick` with `router.push`) navigating to `/admin/${org.slug}`.

Use shadcn `Table`, `TableHeader`, `TableRow`, `TableHead`, `TableBody`, `TableCell` components for the markup. Follow the same import/usage pattern as `client-table.tsx`.
  </action>
  <verify>
1. `npm run build` passes with no errors
2. Navigate to `/admin` as a super-admin user — should see all organisations in a sortable table
3. Clicking "Plan" or "Status" column headers sorts the table
4. Subscription status shows as color-coded badges (green for active, blue for trial, amber for past_due, red for cancelled/unpaid, grey for none)
5. Trial expiry column shows "X days left" for trialing orgs and "—" for non-trialing orgs
6. Client and user count columns show correct numbers
7. Clicking a row navigates to `/admin/[slug]`
8. Navigating to `/admin` as a regular user redirects to `/dashboard`
  </verify>
  <done>
Super-admin org list page shows every organisation with name, slug, plan tier (sortable), subscription status badge (sortable), trial expiry (relative), client count, and user count. Non-super-admin users are silently redirected. The middleware bypass ensures admin routes work without org membership validation.
  </done>
</task>

</tasks>

<verification>
1. Build check: `npm run build` completes without errors
2. Auth guard: Non-super-admin user at `/admin` is redirected to `/dashboard`
3. Unauthenticated user at `/admin` is redirected to `/login`
4. Super-admin sees the "Admin" link in the navigation bar; regular users do not
5. The org list table displays all organisations with correct data from the admin client (service role)
6. Sorting works on plan tier and subscription status columns only
7. Status badges follow the DESIGN.md traffic light system
8. Trial expiry shows relative days, not absolute dates
9. Existing middleware behavior for non-admin routes is unchanged
</verification>

<success_criteria>
- `/admin` route is accessible only to users with `is_super_admin = true` in app_metadata
- The org list displays every organisation with all 7 required columns
- The table is sortable by plan tier and subscription status
- Status badges are color-coded using the existing traffic light system
- Trial expiry is displayed as relative days ("12 days left", "Expired 3 days ago")
- The "Admin" nav link appears only for super-admin users
- No existing route or middleware behavior is broken
</success_criteria>

<output>
After completion, create `.planning/phases/14-super-admin-dashboard/14-01-SUMMARY.md`
</output>
