---
phase: 23-unified-pricing-experience-with-slider-calculator-and-upgrade-prompts
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-02-27T10:58:33.109Z
---

<current_state>
Phase 23 execute-phase is mid-Wave 3 (Plan 23-04), at the human-verify checkpoint (Task 3).
Plans 23-01, 23-02, 23-03 are fully complete with SUMMARYs. Plan 23-04 Tasks 1-2 are committed.
User gave feedback on the checkpoint — two rounds of fixes applied, more remaining.

The execute-phase orchestrator is running `/gsd:execute-phase 23`. After fixing the remaining
items below, resume the checkpoint flow: get "approved", then finish 23-04 SUMMARY, then
verify_phase_goal step, then update_roadmap, then offer_next.
</current_state>

<completed_work>

## Plans Complete
- 23-01: Remove Firm tier, restructure billing config (Wave 1) — 3 commits
- 23-02: Shared PricingSlider component on marketing/pricing/wizard (Wave 2) — 3 commits
- 23-03: Stripe metered billing for Practice overage (Wave 2) — 3 commits
- 23-04 Task 1: UpgradeModal component + client creation flow integration — commit `9679cbd`
- 23-04 Task 2: Limit-aware CSV import with partial import support — commit `bac071d`

## User Feedback Fixes Applied
- Wizard CSV import: red-highlighted over-limit rows in edit-data step + warning banner — commit `d35f9c9`
- Create client dialog: fetches limit on open, shows red/amber warning, disables Create at limit — commit `051f2c2`
</completed_work>

<remaining_work>

## Dashboard CSV Import Dialog (`app/(dashboard)/clients/components/csv-import-dialog.tsx`)
The dashboard CSV dialog currently uses `createIfMissing=false` (only updates existing clients),
so hard limits don't technically apply. BUT the user wants it to show usage info when opened,
similar to how the wizard shows it. Options:
1. Add informational banner showing "X of Y clients used" when dialog opens (simplest)
2. If dialog should also CREATE new clients (like wizard), add `createIfMissing=true` and the
   full red-row highlighting logic from the wizard

**Ask user**: Does the dashboard CSV import need to create new clients? Currently it only
updates metadata for existing clients. If yes, add `createIfMissing` support + limit rows.
If no, just add an informational usage banner.

## After fixes: Complete the checkpoint
1. Get user "approved" on the checkpoint
2. Create 23-04-SUMMARY.md
3. Update STATE.md and ROADMAP.md
4. Run verify_phase_goal step (verifier disabled per config, so may skip)
5. update_roadmap step
6. offer_next step
</remaining_work>

<decisions_made>

- Wave 2 plans (23-02, 23-03) ran in worktrees but committed to main (shared git)
- 23-03 adapted to Stripe Billing Meter Events API instead of deprecated `createUsageRecord`
- Client limit check in wizard CSV uses browser Supabase client (not server action) for simplicity
- Create dialog fetches limit fresh each time it opens to avoid stale data
- "Near limit" threshold set to 3 clients remaining (shows amber warning)
- Dashboard CSV dialog: deferred decision on whether it needs createIfMissing
</decisions_made>

<blockers>
- User feedback loop still open — need confirmation on dashboard CSV dialog behavior
</blockers>

<context>
The execute-phase orchestrator was at the checkpoint_handling step of execute_waves for Wave 3.
The checkpoint was presented to the user, they gave feedback twice:
1. First: CSV import should warn during review (not just results) — fixed with red rows
2. Second: Create client dialog should warn before submission + dashboard CSV needs limits too

The create dialog fix is committed. The dashboard CSV dialog fix is the remaining item.
After user approves, the orchestrator needs to: finish 23-04 (SUMMARY + state), then
aggregate_results, then verify_phase_goal, then update_roadmap, then offer_next.
</context>

<next_action>
1. Ask user about dashboard CSV dialog — does it need to create new clients or just show usage info?
2. Implement the answer
3. Present checkpoint for final approval
4. On "approved": create 23-04-SUMMARY.md, update STATE.md/ROADMAP.md
5. Continue execute-phase workflow: aggregate_results → verify_phase_goal → update_roadmap → offer_next
</next_action>
