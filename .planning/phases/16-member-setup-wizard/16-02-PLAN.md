---
phase: 16-member-setup-wizard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/setup/wizard/components/csv-import-step.tsx
autonomous: true
requirements:
  - SETUP-WIZARD

must_haves:
  truths:
    - "CsvImportStep renders the full CSV import flow (upload → mapping → edit-data → importing → results) without a Dialog wrapper"
    - "CsvImportStep accepts an onComplete() prop called after the user finishes (or skips) the import"
    - "The existing csv-import-dialog.tsx is NOT modified"
    - "All parsing, mapping, and import logic is identical to the dialog — no hand-rolled alternatives"
  artifacts:
    - path: "app/(auth)/setup/wizard/components/csv-import-step.tsx"
      provides: "Full-page CSV import step for wizard"
      contains: "CsvImportStep"
      min_lines: 150
  key_links:
    - from: "app/(auth)/setup/wizard/components/csv-import-step.tsx"
      to: "app/actions/csv.ts (importClientMetadata)"
      via: "direct import"
      pattern: "importClientMetadata"
    - from: "app/(auth)/setup/wizard/page.tsx"
      to: "CsvImportStep"
      via: "import"
      pattern: "CsvImportStep"
---

<objective>
Extract the inner logic of CsvImportDialog into a standalone full-page CsvImportStep component for use in the wizard.

Purpose: The wizard needs the CSV import flow rendered as a full page (inside a Card, not a Dialog). The existing CsvImportDialog must remain unchanged because it is still used on the /clients page. This plan creates a separate component that duplicates the state machine without the Dialog wrapper.

Output: `app/(auth)/setup/wizard/components/csv-import-step.tsx` — a self-contained client component with the same import flow as the dialog, wrapped in Cards instead of DialogContent.
</objective>

<execution_context>
@C:/Users/ejsch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ejsch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/(dashboard)/clients/components/csv-import-dialog.tsx
@app/actions/csv.ts
@.planning/phases/16-member-setup-wizard/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CsvImportStep component (full-page extraction from dialog)</name>
  <files>app/(auth)/setup/wizard/components/csv-import-step.tsx</files>
  <action>
Read app/(dashboard)/clients/components/csv-import-dialog.tsx in full before writing this file. The CsvImportStep component must duplicate ALL internal state and logic from CsvImportDialog but replace Dialog-specific UI with Card-based full-page layout.

Create `app/(auth)/setup/wizard/components/csv-import-step.tsx` as a "use client" component.

INTERFACE:
```typescript
interface CsvImportStepProps {
  onComplete: () => void; // Called when user finishes or skips the import
}

export function CsvImportStep({ onComplete }: CsvImportStepProps)
```

KEEP (copy from CsvImportDialog):
- All imports EXCEPT Dialog-related ones (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose)
- All type definitions: DialogState (rename to StepState), ColumnMapping, ParsedCsvData, EditableRow
- All state variables: stepState (was dialogState), file, parsedData, columnMapping, editableRows, importResult, isImporting, error
- All handler functions: parseFile, autoSuggestMapping, handleFileChange, handleDrop, handleDragOver, handleProceedWithMapping, handleImport, resetState
- All rendering logic for each state (upload, mapping, edit-data, importing, results)

CHANGE:
- Remove: `open`/`onOpenChange` props — this component is always mounted when visible
- Remove: `handleOpenChange` function — the wizard controls mounting/unmounting
- Replace: Dialog/DialogContent wrapper → `<div className="space-y-6">`
- Replace: DialogHeader → `<div className="space-y-1">` with h2 + p for title/description
- Replace: DialogFooter → `<div className="flex justify-end gap-3">`
- Replace: All `onOpenChange(false)` calls (Cancel buttons, close on complete) → `onComplete()`
- Replace: The final "Done" button after successful import (results state) → calls `onComplete()`

WIZARD-SPECIFIC BEHAVIOUR:
- The "Cancel" button on the upload state should read "Skip for now" and call `onComplete()` — allow users to proceed without importing
- No "Cancel" button on mapping/edit-data states — user must complete or go back through the states
- On results state: single button "Next: Configure" that calls `onComplete()`

ANTI-PATTERN WARNING: Do NOT modify csv-import-dialog.tsx. Keep it 100% unchanged.

The file must import importClientMetadata from "@/app/actions/csv" and use papaparse + xlsx exactly as the dialog does.
  </action>
  <verify>
1. `ls "app/(auth)/setup/wizard/components/csv-import-step.tsx"` — file exists
2. `grep -n "CsvImportStep" "app/(auth)/setup/wizard/components/csv-import-step.tsx"` — shows export
3. `grep -n "Dialog" "app/(auth)/setup/wizard/components/csv-import-step.tsx"` — returns ZERO lines (no Dialog imports)
4. `grep -n "importClientMetadata" "app/(auth)/setup/wizard/components/csv-import-step.tsx"` — shows import
5. `npx tsc --noEmit 2>&1 | grep "csv-import-step"` — no errors for this file
  </verify>
  <done>CsvImportStep is a standalone full-page component that runs the complete CSV import flow (upload → mapping → edit-data → importing → results) using Card wrappers. onComplete() is called when the user finishes or skips. csv-import-dialog.tsx is unmodified.</done>
</task>

</tasks>

<verification>
1. `grep -c "Dialog" "app/(auth)/setup/wizard/components/csv-import-step.tsx"` returns 0 (no Dialog usage)
2. Component exports `CsvImportStep` with `onComplete: () => void` prop
3. `npx tsc --noEmit` passes (no type errors)
4. `diff app/(dashboard)/clients/components/csv-import-dialog.tsx app/(dashboard)/clients/components/csv-import-dialog.tsx` — original dialog unchanged (check git diff shows no changes to that file)
</verification>

<success_criteria>
- CsvImportStep component exists at app/(auth)/setup/wizard/components/csv-import-step.tsx
- Implements the full import state machine without any Dialog imports
- Accepts onComplete prop, calls it on finish/skip
- TypeScript compiles without errors
- csv-import-dialog.tsx is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/16-member-setup-wizard/16-02-SUMMARY.md`
</output>
