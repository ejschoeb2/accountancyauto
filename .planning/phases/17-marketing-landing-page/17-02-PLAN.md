---
phase: 17-marketing-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - components/marketing/nav.tsx
  - components/marketing/hero-section.tsx
  - components/marketing/features-section.tsx
  - components/marketing/pricing-section.tsx
  - components/marketing/footer-section.tsx
autonomous: true
requirements: [MKT-SECTIONS]

must_haves:
  truths:
    - "Sticky nav bar at top with Brain logo, section anchor links, Login button, and Sign Up CTA"
    - "Hero section shows 'The Chase Is Over' headline with subtext on the left, particles on the right"
    - "Features section shows 3 cards summarising the software's core functions"
    - "Pricing section shows 3 tier cards (Sole Trader, Practice, Firm) with prices and Start Free Trial CTAs"
    - "Footer section has dark background, 'Ready to stop chasing?' headline, CTA, footer particles, and nav links"
  artifacts:
    - path: "components/marketing/nav.tsx"
      provides: "Sticky navigation bar with anchor links and CTA buttons"
      exports: ["MarketingNav"]
    - path: "components/marketing/hero-section.tsx"
      provides: "Hero section with headline, subtext, CTA, and HeroParticles"
      exports: ["HeroSection"]
    - path: "components/marketing/features-section.tsx"
      provides: "Three feature cards describing core product value"
      exports: ["FeaturesSection"]
    - path: "components/marketing/pricing-section.tsx"
      provides: "Three pricing cards with tier details and CTAs"
      exports: ["PricingSection"]
    - path: "components/marketing/footer-section.tsx"
      provides: "Dark footer with headline, CTA, FooterParticles, and nav links"
      exports: ["FooterSection"]
  key_links:
    - from: "components/marketing/hero-section.tsx"
      to: "components/marketing/hero-particles.tsx"
      via: "import HeroParticles"
      pattern: "HeroParticles"
    - from: "components/marketing/footer-section.tsx"
      to: "components/marketing/footer-particles.tsx"
      via: "import FooterParticles with IntersectionObserver trigger"
      pattern: "FooterParticles.*isTriggered"
    - from: "components/marketing/pricing-section.tsx"
      to: "/onboarding"
      via: "CTA link href"
      pattern: "href.*onboarding"
---

<objective>
Create all five marketing page sections: navigation bar, hero, features, pricing, and footer.

Purpose: Each section is a self-contained client component that composes into the final landing page. Building them as separate files keeps each focused and maintainable.

Output: Five production-ready section components ready for page assembly.
</objective>

<execution_context>
@C:/Users/ejsch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ejsch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-marketing-landing-page/17-CONTEXT.md
@.planning/phases/17-marketing-landing-page/17-RESEARCH.md
@.planning/phases/17-marketing-landing-page/17-01-SUMMARY.md
@app/globals.css (colour variables and theme)
@DESIGN.md (status colours and UI patterns — for reference only, do NOT use dashboard components)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nav.tsx, hero-section.tsx, and features-section.tsx</name>
  <files>components/marketing/nav.tsx, components/marketing/hero-section.tsx, components/marketing/features-section.tsx</files>
  <action>
All three files must have `"use client"` at top.

**nav.tsx — Sticky navigation bar:**
- Export `MarketingNav` component
- Sticky position: `sticky top-0 z-50 backdrop-blur-md bg-white/80 border-b border-border`
- Left: Prompt logo — Lucide `Brain` icon (violet-600) + "Prompt" text
- Center/right: Anchor links to `#features` and `#pricing` sections (standard `<a>` tags with `href="#features"` etc.)
- Right: "Login" text link (`href="/login"`) + "Start Free Trial" CTA button (`href="/onboarding"`)
- Use plain HTML/Tailwind (NOT shadcn Button or Card) — this is a marketing page, not the dashboard
- CTA button styling: violet-600 background, white text, rounded-lg, px-5 py-2.5, hover:violet-700 transition
- Responsive: On mobile (< md), collapse nav links into a simpler layout — either hide anchor links and show only logo + CTA, or use a simple layout. Claude's discretion on mobile nav approach (a hamburger menu is unnecessary for 2 anchor links — just stack or hide).
- Max-width container: `max-w-6xl mx-auto px-6`

**hero-section.tsx — Hero with particles:**
- Export `HeroSection` component
- Two-column layout on desktop (lg:grid-cols-2), single column on mobile
- Left column:
  - "The Chase Is Over" as large bold heading (text-5xl lg:text-6xl font-bold tracking-tight)
  - Subtext below (Claude's discretion — something like "Automated client reminders for UK accounting practices. Stop manually chasing records and documents — Prompt handles it for you.")
  - "Start Free Trial" CTA button (`href="/onboarding"`) — same violet styling as nav CTA but larger (text-lg, px-8 py-3)
  - Below CTA: small "14-day free trial. No card required." text in muted-foreground
- Right column: Container for `<HeroParticles />` import from `@/components/marketing/hero-particles`
  - The right column needs `position: relative` and `overflow: visible` to allow particles to move freely
  - Minimum height: `min-h-[500px]` on desktop for particle space
  - Hidden on mobile (particles are hidden via useIsMobile, but the column itself should also collapse)
- Section padding: `py-20 lg:py-32`
- Section needs `id` attribute for potential future anchoring but hero is at top so not strictly needed
- Hero headline colour: `text-foreground` (dark grey #1a1a1a — NOT text-primary navy)

**features-section.tsx — Three feature cards:**
- Export `FeaturesSection` component
- Section has `id="features"` for anchor link
- Section heading: "What We Do" centered, text-3xl lg:text-4xl font-bold
- Subtext below heading (optional, Claude's discretion)
- 3 cards in a grid: `grid grid-cols-1 md:grid-cols-3 gap-8`
- Each card: white background, rounded-xl, border, p-8, subtle shadow-sm
- Each card has: icon (Lucide, relevant to feature), title, description paragraph
- Card content (Claude's discretion on exact wording — summarise 3 core functions):
  1. **Automated Reminders** — The system sends client reminders automatically based on deadlines and schedules
  2. **Deadline Tracking** — Track Corporation Tax, VAT, Self Assessment, and Companies House deadlines
  3. **Email Management** — Professional email templates, inbound processing, and full audit trail
- Card icons should use status colours as accents (e.g., the reminder card icon in status-info blue, deadline card icon in status-warning amber, email card icon in green-500)
- Cards should NOT use shadcn Card component — use plain divs with Tailwind
- Section padding: `py-20 lg:py-24`
- Background: slightly different from white to create visual separation — `bg-muted/30` or similar subtle tint
  </action>
  <verify>Run `npx tsc --noEmit` to verify TypeScript compilation. Verify each file has "use client" directive. Verify hero-section.tsx imports HeroParticles from the correct path.</verify>
  <done>Nav shows Brain logo + Prompt name, anchor links, Login, and CTA. Hero shows "The Chase Is Over" + subtext + CTA on left, particles container on right. Features shows 3 cards with icons describing the product's core value.</done>
</task>

<task type="auto">
  <name>Task 2: Create pricing-section.tsx and footer-section.tsx</name>
  <files>components/marketing/pricing-section.tsx, components/marketing/footer-section.tsx</files>
  <action>
Both files must have `"use client"` at top.

**pricing-section.tsx — Three pricing cards:**
- Export `PricingSection` component
- Section has `id="pricing"` for anchor link
- Section heading: "Simple, Honest Pricing" centered, text-3xl lg:text-4xl font-bold
- Optional subtext (Claude's discretion)
- 3 cards in grid: `grid grid-cols-1 md:grid-cols-3 gap-8`
- **IMPORTANT: Hardcode all values — do NOT import from lib/stripe/plans.ts** (avoids coupling to Stripe env vars)
- No highlighted/recommended tier — all cards have equal visual weight (per CONTEXT.md decision)
- Card content for each tier:
  1. **Sole Trader** — £39/mo, Up to 40 clients, 1 user
  2. **Practice** — £89/mo, Up to 150 clients, Up to 5 users
  3. **Firm** — £159/mo, Unlimited clients, Unlimited users
- Card layout: white background, rounded-xl, border, p-8
  - Tier name as card title (text-xl font-bold)
  - Price: large (text-4xl font-bold) with "£" prefix and "/mo" suffix in muted text
  - Below price: client limit and user limit as short lines (text-sm text-muted-foreground)
  - NO feature checklists (per CONTEXT.md: "no feature checklists")
  - "Start Free Trial" CTA button at bottom — `<a href="/onboarding">` styled as violet button (same style as nav/hero CTAs)
- Cards should NOT use shadcn Card component — plain divs with Tailwind
- Section padding: `py-20 lg:py-24`
- VAT note below cards: small muted text "All prices exclude VAT."

**footer-section.tsx — Dark footer with particles:**
- Export `FooterSection` component
- Dark background: `bg-[#1a1a1a] text-white`
- Position: relative (for particle overlay)
- **IntersectionObserver trigger for FooterParticles:**
  ```typescript
  const [shouldExplode, setShouldExplode] = useState(false);
  const footerRef = useRef<HTMLElement>(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.intersectionRatio >= 0.4) setShouldExplode(true);
      },
      { threshold: 0.4 }
    );
    const el = footerRef.current;
    if (el) observer.observe(el);
    return () => observer.disconnect();
  }, []);
  ```
- Pass `shouldExplode` as `isTriggered` to `<FooterParticles isTriggered={shouldExplode} />`
- FooterParticles is positioned absolutely inside the footer — imported from `@/components/marketing/footer-particles`
- Main content (centered, z-10 relative so it sits above particles):
  - Bold headline: "Ready to stop chasing?" (text-3xl lg:text-5xl font-bold text-white, text-center)
  - "Start Free Trial" CTA button below headline (`href="/onboarding"`) — white background, violet-600 text (inverted from the main page CTAs for contrast on dark background)
- Below main CTA, navigation links section:
  - Prompt logo (Brain icon in violet + "Prompt" text in white)
  - Link groups: Features (#features), Pricing (#pricing), Login (/login), Sign Up (/onboarding)
  - Legal links (Claude's discretion): Privacy Policy, Terms of Service (can be `#` placeholder hrefs)
  - Small copyright line: "© 2026 Prompt. All rights reserved."
- Section padding: `py-20 lg:py-32` (generous to give particles room)
- Min height: `min-h-[500px]` to ensure particles have space
  </action>
  <verify>Run `npx tsc --noEmit` to verify TypeScript compilation. Verify footer-section.tsx imports FooterParticles. Verify pricing-section does NOT import from lib/stripe/plans.ts. Verify all CTA links point to /onboarding.</verify>
  <done>Pricing section shows 3 equal-weight tier cards with hardcoded prices, limits, and "Start Free Trial" CTAs. Footer section has dark background, "Ready to stop chasing?" headline, CTA, particle effects triggered by IntersectionObserver, and navigation links.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. All 5 section components have `"use client"` directive
3. All CTA buttons/links point to `/onboarding`
4. Nav has Brain icon, anchor links (#features, #pricing), Login link, CTA
5. Hero has "The Chase Is Over" headline and imports HeroParticles
6. Features has 3 cards with icons and descriptions
7. Pricing has 3 cards with hardcoded values — NO import from lib/stripe/plans.ts
8. Footer has dark background, "Ready to stop chasing?" headline, FooterParticles with IntersectionObserver trigger
9. No imports from shadcn/ui components (Card, Button, etc.) — all plain HTML/Tailwind
</verification>

<success_criteria>
- All 5 marketing section components export correctly
- Nav is sticky with glassmorphism effect
- Hero section has two-column layout with particle container
- Feature cards describe 3 core product functions
- Pricing cards show Sole Trader/Practice/Firm with prices and limits
- Footer has dark bg, bold CTA, particles from corners, and nav links
- All CTAs lead to /onboarding
- No dashboard UI component imports (shadcn Card, Button, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/17-marketing-landing-page/17-02-SUMMARY.md`
</output>
